# DEPRECATED: This file is no longer a configuration source.
#
# The generated .env (default: ${ARR_STACK_DIR}/.env) is produced by
# ./arr.sh based on arrconf/userr.conf.defaults.sh combined with
# your overrides in ${ARR_BASE}/userr.conf (default: ${HOME}/srv/userr.conf).
#
# Edit ${ARR_BASE}/userr.conf to change settings, then rerun ./arr.sh.
# Do not edit this file or rely on it for defaults; it will be removed in a
# future release once downstream tooling has migrated.

# Key runtime defaults documented for reference
GLUETUN_CONTROL_PORT=8000
GLUETUN_API_KEY=
WG_FALLBACK_TIMEOUT_SECONDS=120
PORT_MANAGER_ENABLE=0
PM_STATUS_FILE=/tmp/gluetun/forwarded_port
PM_POLL_SECONDS=5
PM_DRY_RUN=0
PM_LOG_LEVEL=info
QBT_USER=admin
QBT_PASS=adminpass
QBT_WEB_PORT=8080
